--- a/app/api/synthesize/route.ts
+++ b/app/api/synthesize/route.ts
@@ -120,6 +120,8 @@ async function generateReviewWithClaude(dataUsed: any, extractedData: any, mana
 Base the content on the available data sources and be specific about what informed each section.`
 
   try {
+    console.log('DEBUG: About to call Claude API, API key exists:', !!process.env.ANTHROPIC_API_KEY)
+    console.log('DEBUG: API key starts with:', process.env.ANTHROPIC_API_KEY?.substring(0, 10))
     const response = await fetch('https://api.anthropic.com/v1/messages', {
       method: 'POST',
       headers: {
@@ -131,6 +133,7 @@ async function generateReviewWithClaude(dataUsed: any, extractedData: any, mana
         model: 'claude-3-haiku-20240307',
         max_tokens: 2000,
         messages: [{
+    console.log('DEBUG: Claude API response status:', response.status)
     if (!response.ok) {
       console.error(`Claude API error: ${response.status}`)
       throw new Error(`Claude API error: ${response.status}`)
